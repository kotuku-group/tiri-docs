// VERIFIED: 2026-08-21

[[ch-origo]]
== Origo
indexterm:[Origo] Origo is the command-line executable that runs Tiri scripts.  It initialises the Kōtuku framework, parses command-line options, resolves the target script, passes arguments to the script environment, and manages the execution lifecycle.

On Linux systems the `tiri` symlink can also be used as an alternative.

[discrete]
[[sec-usage]]
=== Usage

[source,bash]
----
origo [options] [script.tiri] [arg1] [arg2=value] ...
----

If no script path is provided, Origo attempts automatic detection (see <<sec-automatic-script-detection>>).  Options are case-insensitive and begin with `--`.  Unrecognised options beginning with `--` are silently ignored.  All arguments after the script path are forwarded to the script as parameters accessible through `arg()` (see <<sec-script-parameters>>).

[discrete]
[[sec-commandline-options]]
=== Command-Line Options
[discrete]
==== General

[cols="2,4a",options="header"]
|===
|indexterm:[Origo, command-line options] Option |Description

|`--help`
|Prints usage information and exits.

|`--version`
|Prints version information on three lines — version number, git branch and commit hash, and build type — then exits.

|`--time`
|Prints the script execution time in seconds to stdout after the script completes.

|`--dialog`
|Displays an interactive file dialog for choosing a script to execute.

|`--statement <code>`
|Executes a single Tiri statement or expression instead of loading a script file.  The next argument is treated as the code to execute.

|`--procedure <name>`
|Specifies the name of a procedure/function within the script to execute.

|===

[discrete]
==== Logging

All log output is directed to stderr.  Command-line flags control the minimum visible log level; each flag enables its level and all levels above it.  See <<sec-log-levels>> for the full level hierarchy and available flags.

When debugging thread-related issues, append `--log-threads` to include thread identification in log output.

[discrete]
[[sec-origo-jit-options]]
==== JIT Options

indexterm:[JIT options] The `--jit-options` flag accepts a comma-separated list of options that control the compiler, parser diagnostics, and profiling subsystems.  These options are system-wide — they apply to every Tiri script executed during the process lifetime.  Alternatively, per-script options can be set through the `JitOptions` field on the Script object.

[source,bash]
----
origo --jit-options trace-tokens,profile myscript.tiri
----

NOTE: Enabling `trace` or `profile` automatically raises the log level to `--log-api` if it is not already at that level or higher.  For other options, ensure an appropriate log level is set so that diagnostic output is visible.

[discrete]
===== Tracing

The `trace` option is a convenience flag that enables all of the individual trace options listed below.  Individual options can be combined to narrow the output to specific compiler stages.

[cols="2,4a",options="header"]
|===
|Option |Description

|`trace`
|Enables all trace options below in a single flag.

|`trace-tokens`
|Logs tokens as they are produced by the lexer.

|`trace-expect`
|Logs parser expectation checks — useful for diagnosing parse failures.

|`trace-boundary`
|Logs statement and expression boundary detection during parsing.

|`trace-operators`
|Logs operator parsing and precedence decisions.

|`trace-registers`
|Logs register allocation during code generation.

|`trace-cfg`
|Logs control-flow graph construction.

|`trace-assignments`
|Logs assignment analysis during compilation.

|`trace-value-category`
|Logs value-category (lvalue/rvalue) classification of expressions.

|`trace-types`
|Logs type inference and type-checking decisions.
|===

[discrete]
[%breakable]
===== Diagnostics

[cols="2,4a",options="header"]
|===
|Option |Description

|`diagnose`
|Enables diagnostic mode.  The parser captures detailed variable information during compilation, which can be inspected programmatically through the Script object.  Errors are non-fatal so that as many diagnostics as possible are collected in a single pass.

|`dump-bytecode`
|Prints the generated bytecode to the log after compilation completes.

|`profile`
|Enables the parser profiler, which measures time spent in each compiler stage and reports the results.
|===

[discrete]
===== Tips

The tip system provides style and correctness suggestions during parsing.  Tips are categorised by priority; higher levels include all lower-priority tips.

[cols="2,4a",options="header"]
|===
|Option |Description

|`top-tips`
|Shows only the highest-priority tips (level 1) — critical suggestions that are almost always worth addressing.

|`tips`
|Shows priority levels 1 and 2 — the recommended default for routine development.

|`all-tips`
|Shows every tip at all priority levels, including minor stylistic observations.
|===

[discrete]
===== Control

[cols="2,4a",options="header"]
|===
|Option |Description

|`off`
|Disables JIT compilation entirely.  Scripts run in the interpreter.
|===

[discrete]
==== Framework

[cols="2,4a",options="header"]
|===
|Option |Description

|`--gfx-driver=<name>`
|Specifies the graphics driver instead of accepting the default.  Use `--gfx-driver=headless` for automated testing and CI environments where no display is available.

|`--set-volume <name>=<path>`
|Defines or overrides a filesystem volume at startup.  Can be specified multiple times.  Volume paths can reference other volumes.  See <<sec-virtual-volumes>> for further detail on volumes.

|`--no-crash-handler`
|Disables the default crash handler.  On Unix systems, this disables signal handlers for SIGINT, SIGTERM, SIGSEGV, and related signals.  Recommended when debugging with `gdb` and programs that may need to process these signals.

|`--home=<name>`
|Customises the name of the home folder directory (default: `kotuku`).  This segregates user configuration and cache data.

|`--jit-options <options>`
|Passes comma-separated options to the JIT compiler.
|===

[discrete]
[[sec-inline-execution]]
=== Inline Execution
indexterm:[Origo, inline execution] The `--statement` option executes Tiri code directly without loading a script file:

[source,bash]
----
origo --statement "print('Hello World')"
----

[discrete]
[[sec-automatic-script-detection]]
=== Automatic Script Detection
indexterm:[Origo, script detection] When no script path is provided on the command line, Origo searches for a script to execute in the following order:

. If `package.zip` exists in the executable's directory, a `package:` volume is created from the archive and `package:main.tiri` is executed.
. If `main.tiri` exists in the current working directory, it is executed.
. If neither is found, the default Origo window is displayed.
. If no display is available, the help message is printed.

To distribute a packaged Tiri application, copy the Origo executable under the application name (e.g. `myapp.exe`) and place `package.zip` or `main.tiri` alongside it.  The renamed executable runs the application without command-line parameters.

[discrete]
[[sec-script-arguments]]
=== Script Arguments
indexterm:[Origo, script arguments] Arguments after the script path are passed to the script and accessed through the `arg()` function (see <<sec-script-parameters>>).  Three forms are supported:

[cols="2,3a,2a",options="header"]
|===
|Form |Meaning |Access

|`flag`
|Boolean flag, set to `'1'`.
|`arg('flag')` returns `'1'`

|`key=value`
|Named parameter with a string value.
|`arg('key')` returns `'value'`

|`key={ item1 item2 }`
|Array parameter with indexed elements.
|`arg('key(0)')` returns `'item1'`; `arg('key:size')` returns the element count.
|===

All argument values are strings.  Use `tonumber()` for numeric conversion.

[source,bash]
----
origo myscript.tiri verbose width=1024 files={ a.txt b.txt }
----

[source,tiri]
----
if arg('verbose') then print('Verbose mode') end
width = tonumber(arg('width', '800'))   -- 1024
first = arg('files(0)')                 -- 'a.txt'
count = arg('files:size')               -- '2'
----

[discrete]
[[sec-shebang]]
=== Shebang Support
indexterm:[shebang] On Unix systems, Tiri scripts can be made directly executable by adding a shebang line:

[source,tiri]
----
#!/usr/bin/env origo
print('Hello from a Tiri script')
----

Mark the script as executable with `chmod +x script.tiri` to run it directly from the shell.

[discrete]
[[sec-exit-codes]]
=== Exit Codes
Origo returns `0` on successful execution.  A non-zero exit code indicates that the script or framework encountered an error.

If the script sets an explicit error code on its Script object, that error code is returned to the shell.  If the script sets only an error message (with no explicit error code), Origo returns a generic failure code.

[discrete]
[[sec-version-output]]
=== Version Output
The `--version` option prints three lines:

[source,bash]
----
$ origo --version
Kotuku 2026.2.27 <1>
master:5edae466 <2>
Build Type: Debug <3>
----
<1> Framework version.
<2> The git branch and commit hash of the build.
<3> The build type (`Debug` or `Release`).
