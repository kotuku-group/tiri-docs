extends: default

font:
  catalog:
    Noto Serif:
      normal: ../fonts/NotoSerif-Regular.ttf
      bold: ../fonts/NotoSerif-Bold.ttf
      italic: ../fonts/NotoSerif-Italic.ttf
      bold_italic: ../fonts/NotoSerif-BoldItalic.ttf
    Noto Sans:
      normal: ../fonts/NotoSans-Regular.ttf
      bold: ../fonts/NotoSans-Bold.ttf
      italic: ../fonts/NotoSans-Italic.ttf
      bold_italic: ../fonts/NotoSans-BoldItalic.ttf
    Courier Prime:
      normal: ../../../data/fonts/truetype/Courier Prime.ttf
      bold: ../../../data/fonts/truetype/Courier Prime Bold.ttf
      italic: ../../../data/fonts/truetype/Courier Prime Italic.ttf
      bold_italic: ../../../data/fonts/truetype/Courier Prime Bold Italic.ttf
    Source Code Pro:
      normal: ../../../data/fonts/truetype/SourceCodePro-VariableFont.ttf
      bold: ../../../data/fonts/truetype/SourceCodePro-VariableFont.ttf
      italic: ../../../data/fonts/truetype/SourceCodePro-Italic-VariableFont.ttf
      bold_italic: ../../../data/fonts/truetype/SourceCodePro-Italic-VariableFont.ttf
    Inconsolata:
      normal: ../../../data/fonts/truetype/Inconsolata-VariableFont.ttf
      bold: ../../../data/fonts/truetype/Inconsolata-VariableFont.ttf
      italic: ../../../data/fonts/truetype/Inconsolata-VariableFont.ttf
      bold_italic: ../../../data/fonts/truetype/Inconsolata-VariableFont.ttf
    Noto Emoji:
      normal: ../../../data/fonts/truetype/NotoEmoji-VariableFont.ttf
      bold: ../../../data/fonts/truetype/NotoEmoji-VariableFont.ttf
      italic: ../../../data/fonts/truetype/NotoEmoji-VariableFont.ttf
      bold_italic: ../../../data/fonts/truetype/NotoEmoji-VariableFont.ttf
    M+ 1mn:
      normal: GEM_FONTS_DIR/mplus1mn-regular-subset.ttf
      bold: GEM_FONTS_DIR/mplus1mn-bold-subset.ttf
      italic: GEM_FONTS_DIR/mplus1mn-italic-subset.ttf
      bold_italic: GEM_FONTS_DIR/mplus1mn-bold_italic-subset.ttf
    # Glyph fallbacks for the Tiri Unicode operator aliases (ch05 "Unicode Operator
    # Aliases").  Neither Courier Prime (code blocks) nor Inconsolata (codespans) covers
    # ⧺ U+29FA, ⁇ U+2047, ‥ U+2025 or ▷ U+25B7, and Courier Prime also lacks ↑ U+2191,
    # so those characters would otherwise render as .notdef boxes.
    DejaVu Sans Mono:
      normal: ../fonts/DejaVuSansMono.ttf
      bold: ../fonts/DejaVuSansMono-Bold.ttf
      italic: ../fonts/DejaVuSansMono.ttf
      bold_italic: ../fonts/DejaVuSansMono-Bold.ttf
    DejaVu Sans:
      normal: ../fonts/DejaVuSans.ttf
      bold: ../fonts/DejaVuSans.ttf
      italic: ../fonts/DejaVuSans.ttf
      bold_italic: ../fonts/DejaVuSans.ttf
  # Ordered chain: the monospace fallback is tried first so substituted glyphs keep the
  # metrics of the surrounding code.  DejaVu Sans is the last resort and exists solely
  # for ‥ U+2025, the one alias absent from DejaVu Sans Mono.
  fallbacks:
    - Noto Emoji
    - DejaVu Sans Mono
    - DejaVu Sans

base:
  font-family: Noto Serif
  font-size: 10

codespan:
  font-family: Inconsolata
  font-size: 1.1em

role:
  api:
    font-family: Courier Prime
    font-weight: bold
    font-size: 1.1em
    align: left

code:
  font-family: Courier Prime
  breakable: true
  orphans: 5
  page-break-inside: avoid
  font-size: 0.95em
  line-height: 1.15
  padding: [2, 0, 2, 18]
  border-radius: 0
  # A left rule only.  Note that 'background-color' must name the page colour rather
  # than 'transparent': the converter skips the entire fill-and-stroke step when a block
  # has neither a background nor a non-zero *top* border width (converter.rb:4537-4550),
  # and an array border-width is gated on its first (top) element.
  background-color: '#FFFFFF'
  border-width: [0, 0, 0, 2]
  border-color: '#ddddff'

# Grammar blocks ([source,ebnf], or any listing with the 'ebnf' role) are rendered flat
# by the tiri_ebnf.rb extension: the 'code' font is kept, but the left rule, indent and
# background are suppressed.  Keys added here override 'code' for those blocks only.
#ebnf:
#  font-family: Inconsolata

page:
  margin: [0.75in, 0.75in, 0.75in, 0.75in]

title-page:
  logo:
    top: 5%
    align: right
  title:
    top: 45%
    font-size: 30
    font-style: bold
    line-height: 1.2
  subtitle:
    font-size: 13
    font-style: normal
    font-color: '#666666'
    line-height: 1.2
    margin-top: 8
  authors:
    font-size: 16
    margin-top: 12
  revision:
    font-size: 12
    margin-top: 6

table:
#  stripes: even
  head:
    background-color: '#f5f5f5'
  # Applied to tables carrying the 'compact' role.  Requires the tiri_compact_table.rb
  # extension; without it the role is ignored and the table renders at the normal size.
  # Keys added here override the 'table' category for those tables only.
  compact:
    font-size: 8.5
    cell-padding: 2

# Applied to description lists carrying the 'compact' role.  Requires the
# tiri_compact_list.rb extension; without it the role is ignored and the list renders
# at the normal size.  'font-size' sizes the list as a whole and accepts either points
# or a relative value such as '0.85em'; other keys added here override the
# 'description-list' category for those lists only.
description-list:
  compact:
    font-size: 0.85em
    term-spacing: 2

# Applied to an index section carrying the 'compact' role, by the same extension.  The
# index is typeset from the ambient font and the 'description-list' spacing keys, so
# 'font-size' is the effective control here; other keys added here override the 'index'
# category, e.g. 'columns' to reflow the compacted entries into more columns.
index:
  compact:
    font-size: 0.85em

heading:
  min-height-after: 1in
  part:
    break-before: always
    break-after: always
  h1-text-align: center
  h1-margin-page-top: 280

header:
  height: 0.5in
  font-size: 9
  font-color: '#999999'
  border-bottom-width: 0.5
  border-bottom-color: '#DDDDDD'
  recto:
    right:
      content: '{chapter-title}'
  verso:
    left:
      content: '{chapter-title}'

# Compact table of contents.  Requires the tiri_compact_toc.rb extension; without it the run-in keys are
# ignored and the table of contents falls back to one entry per line.
toc:
  font-size: 9.5
  line-height: 1.2
  indent: 12
  dot-leader:
    levels: 0 1 2 3
  run-in:
    level: 4
    style: range
    separator-content: ', '
    separator-font-color: '#777777'
    pagenum-font-color: '#000000'
    range-separator-content: ' - '
